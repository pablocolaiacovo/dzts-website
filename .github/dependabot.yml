version: 2

updates:
  - package-ecosystem: "npm"
    directory: "/"
    # Dependabot reads this file from the default branch (main), but the project
    # flow is deps -> dev -> release to main, so open PRs against dev.
    target-branch: "dev"
    schedule:
      interval: "weekly"
      day: "monday"
    groups:
      next-ecosystem:
        patterns:
          - "next"
          - "next-*"
          - "@next/*"
          - "eslint-config-next"
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
      sanity:
        patterns:
          - "sanity"
          - "@sanity/*"
          - "next-sanity"
      eslint:
        patterns:
          - "eslint"
          - "eslint-*"
          - "@eslint/*"
          - "@typescript-eslint/*"
      bootstrap:
        patterns:
          - "bootstrap"
          - "bootstrap-icons"
          - "@popperjs/*"
    labels:
      - "dependencies"

  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "dev"
    schedule:
      interval: "weekly"
      day: "monday"
    labels:
      - "dependencies"
      - "ci"
