{
  "name": "dzts-inmobiliaria",
  "private": true,
  "version": "1.0.0",
  "description": "DZTS Inmobiliaria",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --parallel build",
    "lint": "pnpm -r --if-present run lint"
  },
  "packageManager": "pnpm@10.33.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "devDependencies": {
    "react-doctor": "^0.4.2"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "19.2.17",
      "@types/react-dom": "19.2.3"
    }
  }
}
